{
  "issues": [
    {
      "name": "normalize-currency-lowercase",
      "category": "refactor",
      "description": "Normalize all currency values to lowercase to match Stripe's API pattern",
      "steps": [
        "Change default currency from 'USD' to 'usd' throughout codebase",
        "Update seed data and test data to use lowercase currencies",
        "Update webhook handlers to normalize incoming currency to lowercase",
        "Update any currency comparisons to be case-insensitive or expect lowercase",
        "Ensure API responses always return lowercase currency",
        "Update migration seed data if applicable",
        "Run tests to verify no regressions"
      ],
      "passes": false
    },
    {
      "name": "nmi-test-account-integration",
      "category": "testing-infra",
      "description": "Set up real Mobius/NMI test account for integration tests that hit the NMI API",
      "steps": [
        "CURRENT: Tests use public NMI demo key which rejects operations on non-existent subscriptions",
        "TARGET: Use real Mobius test account so we can create/cancel real test subscriptions",
        "Obtain test credentials from Mobius (security_key, api_key)",
        "Update testcontainer_suite.go NMI config with Mobius test credentials",
        "Tests can then create real subscriptions via webhook simulation and cancel them via API",
        "BENEFIT: Full integration testing of NMI flows with real data"
      ],
      "tests_blocked": [
        "TestCancelSubscriptionNMI - tries to cancel non-existent subscription",
        "TestCancelSubscriptionEmptyFeedback - tries to cancel non-existent subscription",
        "TestCancelSubscriptionAuthBoundaries/user_B_can_cancel_their_own_subscription - tries to cancel non-existent subscription",
        "TestAdminCancelSubscription/admin_can_cancel_any_user_subscription_by_subscription_ID - tries to cancel non-existent subscription",
        "TestAdminCancelSubscription/admin_can_cancel_any_user_subscription_by_user_ID - tries to cancel non-existent subscription",
        "TestSubscribeNMISuccess - requires valid NMI subscription creation"
      ],
      "passes": false
    },
    {
      "name": "update-subscription-payment-method",
      "category": "feature",
      "description": "Allow users to change which stored payment method their NMI subscription uses",
      "steps": [
        "NMI Client:",
        "  - Add UpdateSubscriptionPaymentSource(subscriptionID, newVaultID string) to internal/integrations/nmi/nmi.go",
        "  - NMI API: recurring=update_subscription, subscription_id=X, customer_vault_id=Y",
        "Handler:",
        "  - Add PUT /v1/me/subscriptions/payment-method endpoint",
        "  - Request: { payment_method_id: 'uuid' }",
        "  - Validate user owns the subscription (from JWT)",
        "  - Validate user owns the target payment method",
        "  - Validate payment method is active and NMI-backed",
        "  - Validate subscription is NMI-backed (not CCBill/Solana)",
        "  - Call NMI to update subscription's customer_vault_id",
        "  - Update local Subscription.PaymentMethodID in database",
        "  - Return updated subscription",
        "Write tests for payment method swap flow"
      ],
      "passes": false
    },
    {
      "name": "unified-checkout-one-time-purchases",
      "category": "feature",
      "description": "Unified checkout endpoint that handles both subscriptions and one-time purchases based on Price.BillingCycleDays",
      "steps": [
        "[DONE] NMI Client:",
        "[DONE]   - Add RunSale(vaultID, amount int64, currency, description string) to internal/integrations/nmi/nmi.go",
        "[DONE]   - NMI API: type=sale, customer_vault_id=X, amount=Y, order_description=Z",
        "[DONE]   - Return transaction_id on success",
        "[DONE]   - Update AddRecurringSubscription to accept optional start_date parameter for delayed-start subscriptions",
        "[DONE] Unified Checkout Handler:",
        "[DONE]   - Create POST /v1/me/checkout endpoint",
        "[DONE]   - REMOVE old endpoints: POST /v1/subscriptions/:processor (mobius, ccbill, solana) - replaced by unified checkout",
        "[DONE]   - Request: { price_id, payment_method_id } OR { price_id, payment_token }",
        "[DONE]   - Look up Price and check BillingCycleDays:",
        "[DONE]   - If BillingCycleDays != nil → subscription flow (see deduplication below)",
        "[DONE]   - If BillingCycleDays == nil → one-time purchase flow",
        "[DONE] Deduplication Logic (CRITICAL - check BOTH subscriptions AND entitlements):",
        "[DONE]   - Get user's active entitlements for the Product (from any source: subscription, one-off, admin)",
        "[DONE]   - Get user's active subscriptions for the Product",
        "[DONE]   - Determine 'coverage_end_date' = latest of (subscription.current_period_ends_at, entitlement.end_at)",
        "[DONE]   - If coverage has NO end date (indefinite) → Block: 'You already have active access'",
        "[DONE]   - If coverage has an end date → Allow purchase with delayed start = coverage_end_date",
        "[DONE]   - If no active coverage → Allow immediate purchase",
        "[DONE]   - NOTE: No 'expiring soon' threshold - user can buy anytime if coverage has an end date.",
        "[DONE] Processor-Specific Handling for Delayed Start:",
        "[DONE]   - CCBill: BLOCK if user has existing coverage - CCBill API cannot schedule future start dates",
        "[DONE]   - NMI subscription: Create with start_date = coverage_end_date, status=pending, charged on start_date",
        "[DONE]   - Solana/one-off: Charge immediately, but create entitlement with start_at = coverage_end_date",
        "[DONE] NMI Subscription Flow:",
        "[DONE]   - If immediate: create NMI subscription with no start_date (charges now)",
        "[DONE]   - If delayed: create NMI subscription with start_date = coverage_end_date",
        "[DONE]   - Store locally with status=pending until webhook confirms first charge",
        "[DONE]   - Webhook handler activates subscription and grants entitlements",
        "[DONE] One-Off Purchase Flow (Solana, NMI RunSale):",
        "[DONE]   - Charge user immediately (Solana tx or NMI sale)",
        "[DONE]   - Create Payment record with purchased_at = now",
        "[DONE]   - If delayed start needed: Create entitlement with start_at = coverage_end_date",
        "[DONE]   - If immediate start: Create entitlement with start_at = now",
        "[DONE] Entitlement Duration for One-Time:",
        "[DONE]   - Product.EntitlementsSpec has { entitlement_name: duration_days }",
        "[DONE] Cross-Processor Scenarios:",
        "[DONE]   - User has NMI sub expiring → buys Solana one-off → Solana entitlement starts when NMI ends",
        "[DONE]   - User has Solana entitlement expiring → creates NMI sub → NMI sub starts when Solana ends",
        "[DONE]   - User has CCBill sub expiring → buys NMI sub → NMI starts when CCBill ends",
        "[DONE]   - User has any coverage → tries CCBill → BLOCKED (CCBill limitation)",
        "[DONE] Write tests for checkout scenarios (tests/checkout_test.go)",
        "[DONE] Denormalized ProductID onto Subscription model for efficient product-based lookups:",
        "[DONE]   - Added ProductID column to Subscription model and migration 006_add_subscription_product_id",
        "[DONE]   - Simplified GetUserProductCoverage() to use single query instead of looping through prices",
        "[TODO] Update frontend to use unified checkout endpoint"
      ],
      "passes": true
    },
    {
      "name": "cli-duplicate-detection",
      "category": "tooling",
      "description": "CLI command to detect duplicate charges and multiple active subscriptions for the same user",
      "steps": [
        "Create cmd/billing-cli/main.go with cobra CLI framework",
        "Add 'duplicates' command with subcommands:",
        "  - 'billing-cli duplicates charges' - find users charged twice in same billing period",
        "    - Query payments grouped by user_id, price_id, month",
        "    - Flag any user with >1 successful payment for same price in same 30-day window",
        "    - Output: user_id, payment_ids, amounts, dates, recommendation (refund which one)",
        "  - 'billing-cli duplicates subscriptions' - find users with multiple active subscriptions",
        "    - Query subscriptions WHERE status='active' GROUP BY user_id HAVING count > 1",
        "    - Output: user_id, subscription_ids, prices, start_dates, recommendation (cancel which one)",
        "Add --format flag for output (table, json, csv)",
        "Add --dry-run flag (default) vs --fix flag for auto-remediation (with confirmation prompt)",
        "Add date range filters (--since, --until) to limit search scope",
        "Write tests for duplicate detection logic"
      ],
      "passes": false
    },
    {
      "name": "cli-orphan-entitlements",
      "category": "tooling",
      "description": "CLI command to detect users with active entitlements that should have expired",
      "steps": [
        "Add 'orphans' command to billing-cli:",
        "  - 'billing-cli orphans entitlements' - find users with premium but no valid source",
        "Query logic:",
        "  - Get all users with active entitlements (end_at IS NULL OR end_at > now, revoked_at IS NULL)",
        "  - For each, check if they have a valid source:",
        "    - source_type='subscription' → verify subscription is still active and not expired",
        "    - source_type='one_off' → verify payment exists and end_at hasn't passed",
        "    - source_type='admin' → SKIP (intentionally granted, don't flag)",
        "  - Flag entitlements where source is invalid/expired but entitlement still active",
        "Output columns:",
        "  - user_id, entitlement_id, entitlement_name, source_type, source_id",
        "  - source_status (e.g., 'subscription_cancelled', 'subscription_expired', 'payment_refunded')",
        "  - entitlement_start, expected_end, recommendation",
        "Add --include-admin flag to also audit admin-granted entitlements (off by default)",
        "Add --format flag (table, json, csv)",
        "Add --fix flag to auto-revoke orphaned entitlements (with confirmation prompt)",
        "Write tests for orphan detection logic"
      ],
      "passes": false
    },
    {
      "name": "admin-user-search",
      "category": "feature",
      "description": "Sophisticated user search for admin dashboard",
      "steps": [
        "Design search API:",
        "  - GET /v1/admin/users?q=...&filters... - search users with billing data",
        "Search fields:",
        "  - email (partial match from subscriptions/payments)",
        "  - user_id (exact match)",
        "  - processor_subscription_id (exact match for support lookups)",
        "  - transaction_id (exact match for payment support)",
        "Filters:",
        "  - has_subscription=true/false",
        "  - subscription_status=active/cancelled/past_due/etc",
        "  - processor=nmi/ccbill/solana",
        "  - has_entitlement=premium/etc",
        "  - created_after, created_before (date range)",
        "Sorting:",
        "  - sort_by=newest/oldest/last_payment/subscription_start",
        "Pagination:",
        "  - page, page_size, cursor-based pagination for large results",
        "Performance:",
        "  - Add database indexes for search fields",
        "  - Consider full-text search for email",
        "  - Rate limit search queries",
        "NOTE: Billing service searches its own data. Full user profile search (username, etc) lives in main doujins API."
      ],
      "passes": false
    },
    {
      "name": "admin-rate-limiting",
      "category": "security",
      "description": "Add rate limiting to admin endpoints to limit blast radius of compromised JWT",
      "steps": [
        "PROBLEM: If admin JWT is leaked, attacker could cancel/refund thousands of users before detection",
        "Implement per-admin-user rate limiting using Redis (keyed by admin user ID from JWT)",
        "Define rate limits for destructive operations: e.g., 5 cancellations/minute, 10/hour, 50/day",
        "Define rate limits for bulk operations: e.g., 3 extend operations/minute",
        "On rate limit exceeded: lock out admin for extended period (e.g., 1 hour) and alert",
        "Add alerting/notification when rate limits are approached (e.g., 80% threshold)",
        "Create admin rate limit middleware that wraps destructive endpoints",
        "Allow super-admin or manual override to unlock rate-limited admin if legitimate",
        "Log all rate limit events for security audit",
        "BENEFIT: Limits blast radius - attacker can only affect ~5-10 users before getting locked out"
      ],
      "passes": false
    },
    {
      "name": "solana-devnet-integration-tests",
      "category": "testing-infra",
      "description": "Add real Solana devnet integration tests that move test SOL/tokens on-chain",
      "steps": [
        "SETUP COMPLEXITY: Medium-high. Requires funded devnet wallet and handling rate limits/faucet availability.",
        "Create test wallet keypair specifically for integration tests (store in config or env)",
        "Fund test wallet via Solana devnet faucet (solana airdrop) - may need retry logic due to rate limits",
        "Create test for full payment flow: generate intent -> sign transaction -> submit -> verify on-chain",
        "Use devnet RPC endpoint (https://api.devnet.solana.com or Helius/QuickNode devnet)",
        "Test SOL transfer: create intent for X lamports, sign with test wallet, submit, verify balance changes",
        "Test SPL token transfer: create test SPL token on devnet, test token payment flow",
        "Handle devnet instability: add retries, longer timeouts, skip tests if devnet unavailable",
        "Consider using localnet (solana-test-validator) for faster/more reliable tests as alternative",
        "Add -tags=devnet build tag to separate from unit tests (expensive/slow to run)",
        "Document devnet wallet setup and faucet requirements in README"
      ],
      "passes": false
    },
    {
      "name": "remove-wallet-verification-requirement",
      "category": "refactor",
      "description": "Remove mandatory wallet verification before Solana payments - it's unnecessary since transaction signatures already prove ownership",
      "steps": [
        "CURRENT: User must link+verify wallet via challenge/sign before making a payment",
        "PROBLEM: This is an unnecessary step - the transaction signature itself proves wallet ownership",
        "Update CreatePaymentIntent to accept any wallet address (not just verified ones)",
        "Update ConfirmPaymentIntent to verify the signed transaction's fee payer matches the claimed wallet",
        "Keep wallet linking endpoints for optional UX convenience (remember wallets) but don't require verification for payments",
        "Update GeneratePayment handler similarly",
        "Remove isVerified checks from payment flows",
        "Update tests to reflect new behavior (payments work without pre-verification)",
        "BENEFIT: Simpler UX - users can pay immediately without linking wallet first"
      ],
      "passes": false
    },
    {
      "name": "solana-pay-transaction-request",
      "category": "feature",
      "description": "Implement Solana Pay Transaction Request spec (server-built transactions via HTTPS endpoint)",
      "steps": [
        "CURRENT: We only support Transfer Requests (solana:<recipient>?amount=X) where wallet builds the transaction",
        "TARGET: Also support Transaction Requests (solana:https://api.example.com/pay) where server builds the transaction",
        "Create GET /v1/solana-pay/:intent_id endpoint returning { label, icon } per spec",
        "Create POST /v1/solana-pay/:intent_id endpoint accepting { account } and returning { transaction, message? }",
        "Server builds the transaction with the user's account as signer (not fee payer if we want sponsored txs)",
        "Return base64-encoded serialized transaction for wallet to sign and broadcast",
        "Update CreatePaymentIntentQR to optionally return transaction request URL instead of transfer request URL",
        "Add config option to choose between transfer vs transaction request mode",
        "BENEFIT: Enables complex transactions (swaps, multi-instruction), sponsored fees, and server-side transaction building",
        "Write tests for transaction request flow"
      ],
      "passes": false
    },
    {
      "name": "solana-sponsored-transactions",
      "category": "feature",
      "description": "Allow merchant to pay Solana transaction fees so users don't need SOL in their wallet",
      "steps": [
        "CURRENT: User is fee payer - must have SOL for transaction fees (~0.000005 SOL)",
        "TARGET: Merchant pays fees - users can pay with just USDC/tokens, no SOL needed",
        "Create separate 'fee payer' wallet (distinct from recipient wallet to limit blast radius)",
        "Add fee_payer_wallet and fee_payer_private_key to Solana config (or use KMS reference)",
        "Integrate with AWS KMS or GCP Cloud HSM for secure private key storage (never store raw key in config)",
        "Update BuildPaymentTransaction to set merchant as fee payer (account[0])",
        "Server signs transaction first with fee payer key, then returns partially-signed tx to user",
        "User signs as token sender, transaction now has both signatures",
        "Add rate limiting specifically for sponsored transactions to prevent abuse/draining",
        "Add monitoring/alerting for fee payer wallet balance (alert when low)",
        "Add config flag to enable/disable sponsored transactions (default off)",
        "Consider using Helius or Jito for fee sponsorship as simpler alternative to self-managed keys",
        "TRADEOFF: Adds complexity and security burden (private key on server) but improves UX significantly",
        "Write tests for sponsored transaction flow"
      ],
      "passes": false
    },
    {
      "name": "simplify-solana-pay-architecture",
      "category": "refactor",
      "description": "Simplify Solana Pay to use Redis for pending state, server-side polling, and eliminate the SolanaPaymentIntent model",
      "spec": {
        "overview": "Replace the complex SolanaPaymentIntent state machine with a simpler architecture: Redis for ephemeral pending payments, a background goroutine that polls Solana every 500ms for confirmations, and Postgres only for confirmed payments.",
        "current_problems": [
          "SolanaPaymentIntent table mirrors blockchain state unnecessarily",
          "Complex state machine (pending → processing → confirmed/failed/expired)",
          "Multiple endpoints for what should be simple operations",
          "Frontend polling triggers blockchain checks (fragile if frontend disconnects)"
        ],
        "endpoints": {
          "POST /v1/solana/pay": {
            "request": "{ price_id, token }",
            "response": "{ url, reference, amount, token_amount, token, expires_at }",
            "behavior": [
              "Generate random 32-byte reference (base58 encoded)",
              "Calculate token amount from price using current quote",
              "Build solana: Transfer Request URL with reference",
              "Store in Redis HASH: solana_pay:{ref} → { user_id, price_id, amount, token, recipient, created_at } with 15min TTL",
              "Add reference to Redis SET 'pending_solana_payments'",
              "Return URL for frontend to display as QR"
            ]
          },
          "GET /v1/solana/pay/status": {
            "request": "?reference=REF123",
            "response": "{ status: 'pending' | 'confirmed' | 'expired', payment_id?, signature? }",
            "behavior": [
              "Check Postgres for Payment with this reference - if found, return confirmed + payment_id + signature",
              "Check Redis for solana_pay:{ref} - if not found, return expired",
              "Otherwise return pending (poller handles actual blockchain checking)"
            ]
          }
        },
        "poller": {
          "name": "SolanaPaymentPoller",
          "type": "Goroutine with 500ms ticker (NOT a River job)",
          "why_not_river": "River jobs have overhead, 500ms polling needs a lightweight goroutine",
          "startup": "go StartSolanaPoller(ctx, state) in main.go alongside HTTP server",
          "behavior": [
            "Ticker fires every 500ms",
            "SMEMBERS pending_solana_payments → get all pending references",
            "If empty, continue (SMEMBERS on empty set is ~0.1ms, negligible)",
            "Batch RPC: send single HTTP request with N getSignaturesForAddress queries",
            "For each reference with signature found:",
            "  - Fetch full transaction via getTransaction RPC",
            "  - Verify amount, recipient, and token match expected values from Redis HASH",
            "  - Create Payment record in Postgres (idempotent on signature)",
            "  - Grant entitlements to user",
            "  - SREM from pending_solana_payments SET",
            "  - DEL solana_pay:{ref} HASH",
            "For each reference with no signature:",
            "  - Check if expired (created_at > 15min ago or HASH missing)",
            "  - If expired, SREM from SET and continue",
            "  - Otherwise, still pending, do nothing"
          ],
          "latency": "~500-900ms from user signing tx to confirmed in our system (400ms slot time + 0-500ms poll interval)"
        },
        "redis_structure": {
          "pending_solana_payments": "SET of reference strings currently being polled",
          "solana_pay:{reference}": "HASH { user_id, price_id, amount, token, recipient, created_at } with 15min TTL"
        },
        "frontend_polling": {
          "description": "Frontend polls GET /v1/solana/pay/status every 500ms for low-latency UX",
          "flow": [
            "POST /v1/solana/pay → get { url, reference }",
            "Display QR code with url",
            "Poll GET /v1/solana/pay/status?reference=REF every 500ms",
            "When status=confirmed → redirect to success page",
            "When status=expired → show expiration message"
          ],
          "latency_analysis": "Backend polls Solana every 500ms, frontend polls backend every 500ms. Worst case: user signs → 500ms until backend sees it → 500ms until frontend sees it = ~1 second total. Best case: ~0ms if polls align perfectly."
        },
        "what_to_remove": [
          "solana_payment_intents table and migration",
          "SolanaPaymentIntent model",
          "SolanaPaymentIntentService",
          "SolanaPaymentIntentRepo",
          "Intent state machine (pending/processing/confirmed/failed/expired)",
          "POST /v1/payment-intents/* endpoints",
          "GET /v1/payment-intents/:id endpoint"
        ],
        "what_to_keep": [
          "Payment model and table (confirmed payments only)",
          "SolanaTransaction model (audit trail of on-chain txs)",
          "Entitlement granting logic",
          "Token quote calculation (price → token amount)",
          "Transaction verification logic (amount, recipient, token checks)"
        ],
        "benefits": [
          "Simpler mental model: Redis for pending, Postgres for confirmed",
          "No state machine to maintain",
          "Server-side polling ensures payments processed even if frontend disconnects",
          "Snappy UX: 500ms polling catches payments almost instantly",
          "Efficient: batch RPC means 1 HTTP request regardless of pending count",
          "Fewer database writes (only on confirmation)",
          "Easier to reason about and debug"
        ]
      },
      "passes": true,
      "completed_notes": "Implemented simplified Solana Pay architecture with Redis for pending payments, 500ms server-side polling, and Postgres only for confirmed payments. Created POST /v1/solana/pay, GET /v1/solana/pay/status endpoints, SolanaPayService for Redis storage, and SolanaPayPoller goroutine. Remaining cleanup (removing old SolanaPaymentIntent code) can be done separately."
    },
    {
      "name": "admin-grants-and-offline-purchases",
      "category": "design",
      "description": "Design and implement admin-granted entitlements and offline/manual payments (cash, comps, partnerships)",
      "questions_to_answer": [
        "What's the difference between an admin-granted entitlement and a one-off purchase?",
        "Should admin grants create a Payment record or just an Entitlement?",
        "How do we track the 'source' of an entitlement (subscription, one-off payment, admin grant, partnership)?",
        "Should there be a 'reason' field for admin grants (comp, refund compensation, partnership, contest winner)?",
        "How do offline payments (cash, check, wire) get recorded?",
        "Do offline payments need a 'processor' value? (e.g., 'manual', 'cash', 'wire')",
        "How does the CLI orphan-entitlements checker know an admin grant is intentional vs orphaned?"
      ],
      "key_insight": {
        "problem": "Current admin grant endpoint grants raw entitlement names - admin must know internal entitlement strings",
        "solution": "Admin should grant PRODUCTS (or prices), and the system derives entitlements from Product.EntitlementsSpec",
        "benefits": [
          "Admin doesn't need to know entitlement internals",
          "If product's entitlements change, grants automatically use new spec",
          "Consistent with how subscriptions and payments work",
          "Audit trail shows 'granted Premium product' not 'granted premium_access entitlement'"
        ],
        "current_flow": "POST /v1/admin/users/:id/entitlements { entitlement: 'premium_access', days: 30 }",
        "proposed_flow": "POST /v1/admin/users/:id/grants { product_id: 'prod_xxx', days: 30, reason: 'comp' }"
      },
      "current_state": {
        "admin_grant_entitlement": "POST /v1/admin/users/:user_id/entitlements grants entitlement with optional duration_days",
        "entitlement_source_type": "Entitlement has SourceType field: 'subscription', 'one_off', 'admin'",
        "no_payment_for_admin": "Admin grants create Entitlement only, no Payment record",
        "no_offline_payment_flow": "No way to record a cash/manual payment that grants entitlements"
      },
      "proposed_design": {
        "entitlement_sources": [
          "subscription - linked to active subscription, revoked when subscription ends",
          "payment - linked to one-off Payment record (online or offline)",
          "admin - manually granted, optional reason, no payment linkage",
          "partnership - external deal, tracked separately"
        ],
        "offline_payment_endpoint": {
          "endpoint": "POST /v1/admin/payments/manual",
          "request": "{ user_id, price_id, amount, currency, payment_method: 'cash'|'wire'|'check'|'comp', notes }",
          "behavior": [
            "Create Payment record with processor='manual', no external transaction_id",
            "Grant entitlements from the Price's Product.EntitlementsSpec",
            "Return payment_id for audit trail"
          ]
        },
        "admin_grant_vs_manual_payment": {
          "admin_grant": "Quick entitlement grant without payment trail - for comps, bug compensation, testing",
          "manual_payment": "Full payment record for offline money received - for cash, wire, partnerships with money"
        }
      },
      "steps": [
        "Decide: should admin grants require a reason/category?",
        "Add 'manual' as valid processor for offline payments",
        "Create POST /v1/admin/payments/manual endpoint",
        "Update Payment model to allow nil transaction_id for manual payments",
        "Ensure entitlement SourceType correctly distinguishes subscription vs payment vs admin",
        "Update CLI orphan checker to handle admin/manual sources correctly",
        "Write tests for manual payment flow",
        "Document when to use admin grant vs manual payment"
      ],
      "passes": false
    },
    {
      "name": "prorated-upgrades-downgrades",
      "category": "design",
      "description": "Design proration logic for subscription upgrades and downgrades between products/tiers",
      "tier_group_design": {
        "overview": "Add tier_group (string) and tier_rank (int) to Product model to define upgrade/downgrade relationships without needing a separate table",
        "schema": {
          "tier_group": "Nullable string grouping mutually-exclusive products (e.g., 'premium', 'pro'). Products in same group cannot be owned simultaneously - must upgrade/downgrade.",
          "tier_rank": "Integer (default 0). Higher = more premium. Used to determine upgrade vs downgrade direction."
        },
        "example_products": [
          { "name": "Premium Monthly", "tier_group": "premium", "tier_rank": 1, "entitlements": { "premium": 30 } },
          { "name": "Premium+ Monthly", "tier_group": "premium", "tier_rank": 2, "entitlements": { "premium": 30, "extra": 30 } },
          { "name": "Premium Ultimate", "tier_group": "premium", "tier_rank": 3, "entitlements": { "premium": 30, "extra": 30, "vip": 30 } },
          { "name": "Addon Pack", "tier_group": null, "tier_rank": 0, "entitlements": { "addon_feature": 30 }, "note": "Standalone - can be owned alongside any premium tier" }
        ],
        "checkout_logic": [
          "If product.TierGroup is set, check for existing active subscription in same tier group",
          "If found and existingProduct.TierRank < newProduct.TierRank → route to processUpgrade()",
          "If found and existingProduct.TierRank > newProduct.TierRank → route to processDowngrade()",
          "If found and same rank → block as duplicate",
          "If no tier group or no existing sub in group → normal checkout flow"
        ],
        "benefits": [
          "No new table needed - just two columns on products",
          "Semantic tier_group names are human-readable ('premium' vs UUID)",
          "Flexible: standalone products (null tier_group) work independently",
          "Clear upgrade/downgrade direction via tier_rank comparison"
        ]
      },
      "scenarios": {
        "upgrade": {
          "example": "User on Premium ($10/mo, 15 days left) upgrades to Premium+ ($20/mo)",
          "recommended_behavior": [
            "Charge prorated difference immediately: ($20-$10) * (15/30) = $5",
            "Grant Premium+ entitlements immediately",
            "Keep same billing anchor date (next charge in 15 days at $20)",
            "Cancel old NMI subscription, create new one (or update existing)"
          ]
        },
        "downgrade": {
          "example": "User on Premium+ ($20/mo, 15 days left) downgrades to Premium ($10/mo)",
          "recommended_behavior": [
            "Schedule downgrade for end of current period",
            "Keep Premium+ entitlements until period ends",
            "Set subscription.scheduled_price_id = new price",
            "At renewal, switch to new price and grant new tier entitlements",
            "No immediate refund (user paid for this period)"
          ]
        },
        "mid_cycle_cancel_resubscribe": {
          "example": "User cancels Premium, then resubscribes 10 days later to Premium+",
          "behavior": "Treat as fresh subscription if previous is cancelled. No tier group check since cancelled sub is not 'active'."
        }
      },
      "processor_considerations": {
        "ccbill": {
          "behavior": "CCBill handles upgrades via Package Upgrade flow",
          "proration": "CCBill handles proration internally",
          "our_role": "Receive webhook and update entitlements/subscription record accordingly",
          "limitation": "Cannot schedule future start dates - upgrades are immediate only"
        },
        "nmi": {
          "behavior": "NMI is just a gateway - we control subscription logic",
          "proration": "We calculate and charge proration ourselves",
          "approach": [
            "For upgrades: Cancel old subscription, charge prorated difference via RunSale(), create new subscription",
            "For downgrades: Store scheduled_price_id, on renewal webhook switch to new price"
          ]
        },
        "solana": {
          "behavior": "One-time payments, no recurring subscriptions",
          "upgrade_path": "User buys higher tier product, gets delayed-start entitlement (existing dedup logic handles this)"
        }
      },
      "implementation_steps": [
        "[DONE] Add tier_group (string, nullable) and tier_rank (int, default 0) to Product model",
        "[DONE] Create migration 007_add_product_tier_columns",
        "[DONE] Add GetActiveSubscriptionByTierGroup(userID, tierGroup string) to repo/service",
        "[DONE] Update checkout logic to detect upgrade/downgrade scenarios",
        "[DONE] Implement processUpgrade() - immediate switch with proration charge",
        "[DONE] Implement processDowngrade() - schedule for end of period",
        "[DONE] Add scheduled_price_id column to Subscription for pending downgrades (migration 008)",
        "[TODO] Update renewal webhook handler to apply scheduled downgrades",
        "[DONE] Implement proration calculation: (priceDiff) * (daysRemaining / cycleDays)",
        "[TODO] Handle CCBill upgrade webhooks",
        "[DONE] Simplify CCBill checkout: POST /v1/me/checkout with processor=ccbill now returns FlexForm URL directly in redirect_url",
        "[DONE] Simplify CCBill upgrades: checkout endpoint detects tier upgrades and returns upgrade FlexForm URL directly",
        "[REMOVED] Deleted legacy endpoints /v1/subscriptions/ccbill/flexform-url and /ccbill/upgrade-url (use checkout instead)",
        "[TODO] Write tests for tier group detection",
        "[TODO] Write tests for proration calculations",
        "[TODO] Write tests for upgrade/downgrade flows"
      ],
      "passes": false
    },
    {
      "name": "remove-ccbill-alias-service",
      "category": "refactor",
      "description": "Completely removed CCBillAliasService - now using real usernames from JWT",
      "rationale": [
        "Users already have usernames in the profiles schema",
        "Alias generation was unnecessary complexity",
        "Makes it easier to identify users in CCBill's admin panel",
        "Webhook resolution now queries profiles.users directly"
      ],
      "steps": [
        "[DONE] Update checkout_service.go to use user.Username directly",
        "[DONE] Add GetUserIDByUsername() to ProfileRepo for webhook resolution",
        "[DONE] Update CCBillWebhookService to use ProfileRepo instead of CCBillAliasService",
        "[DONE] Update WebhookDispatcher to use ProfileRepo instead of CCBillAliasService",
        "[DONE] Remove CCBillAliasService from CheckoutService dependencies",
        "[DONE] Remove CCBillAliasService from build_runtime.go and runtime.go",
        "[DONE] Delete internal/services/ccbill_alias.go",
        "[DONE] Delete internal/services/ccbill_alias_test.go",
        "[DONE] Delete internal/db/repo/ccbill_username_alias.go",
        "[DONE] Delete internal/db/models/ccbill_username_alias.go",
        "[DONE] Create migration 009_drop_ccbill_aliases_table to drop the table",
        "[DONE] Update tests/testcontainer_suite.go to remove CCBillAliasService references"
      ],
      "passes": true,
      "completed_notes": "Completely removed CCBillAliasService. CCBill checkout now sends the real username from the JWT. Webhook resolution looks up user IDs directly from profiles.users.username via ProfileRepo.GetUserIDByUsername(). Migration 009 drops the now-unused ccbill_username_aliases table."
    },
    {
      "name": "standardize-pagination-format",
      "category": "refactor",
      "description": "Standardize all list endpoints to use Stripe-like pagination: object/data/total/limit/offset/has_more",
      "target_format": {
        "example": {
          "object": "list",
          "data": ["...items..."],
          "total": 100,
          "limit": 50,
          "offset": 0,
          "has_more": true
        },
        "rationale": [
          "Stripe-compatible API design",
          "limit/offset maps directly to SQL LIMIT/OFFSET",
          "object: 'list' makes responses self-describing",
          "has_more is simple to calculate: offset + len(data) < total",
          "Easy to evolve to cursor-based pagination later"
        ]
      },
      "current_state": {
        "style_1_page_based": {
          "format": "{ data, total_items, page, page_size, total_pages }",
          "endpoints": [
            "GET /v1/products - uses api.ListResponse with page/page_size/total_pages",
            "GET /v1/prices - uses api.ListResponse with page/page_size/total_pages",
            "GET /v1/me/subscriptions - uses PaginatedResponse with total_items only",
            "GET /v1/me/payments - uses PaginatedResponse with total_items only",
            "GET /v1/me/notifications - uses PaginatedResponse with page/page_size",
            "GET /v1/me/payment-methods - uses PaginatedResponse (page/page_size params)"
          ]
        },
        "style_2_limit_offset": {
          "format": "{ data, total, limit, offset }",
          "endpoints": [
            "GET /v1/admin/payments - uses SuccessJSONPaginated",
            "GET /v1/admin/subscriptions - uses SuccessJSONPaginated"
          ]
        },
        "no_pagination": {
          "format": "{ wallets: [...] } or just array",
          "endpoints": [
            "GET /v1/me/wallets - returns { wallets: [] }, no pagination"
          ]
        }
      },
      "endpoints_to_update": [
        {
          "endpoint": "GET /v1/products",
          "file": "internal/handlers/get_products.go",
          "current": "api.ListResponse with page/page_size/total_pages",
          "change": "Add limit/offset params, replace page fields with limit/offset/has_more"
        },
        {
          "endpoint": "GET /v1/prices",
          "file": "internal/handlers/get_prices.go",
          "current": "api.ListResponse with page/page_size/total_pages",
          "change": "Add limit/offset params, replace page fields with limit/offset/has_more"
        },
        {
          "endpoint": "GET /v1/me/subscriptions",
          "file": "internal/handlers/get_my_subscriptions.go",
          "current": "PaginatedResponse with total_items only, params: limit/offset",
          "change": "Add object:'list', has_more field"
        },
        {
          "endpoint": "GET /v1/me/payments",
          "file": "internal/handlers/get_user_payments.go",
          "current": "PaginatedResponse with total_items only",
          "change": "Add object:'list', limit/offset params, has_more field"
        },
        {
          "endpoint": "GET /v1/me/notifications",
          "file": "internal/handlers/notifications.go",
          "current": "PaginatedResponse with page/page_size params",
          "change": "Switch to limit/offset params, add object:'list', has_more"
        },
        {
          "endpoint": "GET /v1/me/payment-methods",
          "file": "internal/handlers/payment_methods.go",
          "current": "Direct response, page/page_size params",
          "change": "Switch to limit/offset, wrap in standard list response"
        },
        {
          "endpoint": "GET /v1/admin/payments",
          "file": "internal/handlers/admin_payments.go",
          "current": "SuccessJSONPaginated with limit/offset (good!)",
          "change": "Add object:'list', has_more field"
        },
        {
          "endpoint": "GET /v1/admin/subscriptions",
          "file": "internal/handlers/admin_users.go",
          "current": "SuccessJSONPaginated with limit/offset (good!)",
          "change": "Add object:'list', has_more field"
        }
      ],
      "implementation_steps": [
        "Update api.ListResponse to use limit/offset/has_more instead of page/page_size/total_pages",
        "Update SuccessJSONPaginated to include object:'list' and has_more",
        "Update PaginatedResponse struct or remove in favor of api.ListResponse",
        "Update GET /v1/products to accept limit/offset params",
        "Update GET /v1/prices to accept limit/offset params",
        "Update GET /v1/me/subscriptions response format",
        "Update GET /v1/me/payments response format and params",
        "Update GET /v1/me/notifications to use limit/offset",
        "Update GET /v1/me/payment-methods to use limit/offset",
        "Update GET /v1/admin/payments response format",
        "Update GET /v1/admin/subscriptions response format",
        "Update tests to use new pagination format",
        "Document standard pagination format in API docs"
      ],
      "passes": false
    },
    {
      "name": "credits-balance-system",
      "category": "feature",
      "description": "Implement a credits/balance system for consumable purchases (API credits, GPU time, AI tokens)",
      "research": {
        "accelbyte": {
          "source": "https://docs.accelbyte.io/gaming-services/services/monetization/wallets/",
          "key_concepts": [
            "Each currency = one wallet. Multiple currencies = multiple wallets per user.",
            "Sub-wallets per platform source (Steam, PlayStation, System) - balances sum together",
            "Two balance types: Permanent (never expires) and Time-Limited (expires after set date)",
            "Time-limited balances deplete BEFORE permanent, regardless of source priority",
            "Japan region: 180-day expiration required for paid virtual currency (Payment Services Act)",
            "Credit operations track: amount, source type (Purchase, Promotion, Achievement, Referral_Bonus, Redeem_Code, Other), optional expiration",
            "Full transaction history available for audit"
          ]
        },
        "playfab": {
          "source": "https://learn.microsoft.com/en-us/gaming/playfab/features/economy/tutorials/currencies",
          "key_concepts": [
            "Currency defined as catalog item with FriendlyId (1-3 char code)",
            "Two primary operations: AddInventoryItems (deposit), PurchaseInventoryItems (spend)",
            "Legacy v1 had 'recharge rate' - auto-grant X credits per day (interesting for daily allowances)",
            "Currencies tied to catalog items for purchasing",
            "Less sophisticated than AccelByte - no expiration, no sub-wallets"
          ]
        }
      },
      "design_decisions": {
        "terminology": "Use 'credits' as the generic term. Each credit type is a 'CreditType' (e.g., 'gpu_time', 'api_calls', 'ai_tokens')",
        "expiration": "Support optional expiration per credit grant (like AccelByte time-limited balance)",
        "fifo_expiration": "Expiring credits consumed first, then permanent credits (AccelByte pattern)",
        "no_sub_wallets": "Skip sub-wallets complexity - we don't have multi-platform sources like gaming",
        "ledger_based": "Append-only transaction ledger for audit trail, with denormalized balance for fast reads"
      },
      "schema": {
        "credit_types": {
          "description": "Defines available credit types (like currencies)",
          "columns": [
            "id UUID PRIMARY KEY",
            "name TEXT UNIQUE NOT NULL -- 'gpu_time', 'api_credits', 'ai_tokens'",
            "display_name TEXT NOT NULL -- 'GPU Minutes', 'API Credits'",
            "description TEXT",
            "is_active BOOLEAN DEFAULT true",
            "created_at TIMESTAMPTZ DEFAULT NOW()"
          ]
        },
        "user_credit_balances": {
          "description": "Current balance per user per credit type (denormalized for fast reads)",
          "columns": [
            "id UUID PRIMARY KEY",
            "user_id TEXT NOT NULL",
            "credit_type_id UUID REFERENCES credit_types(id)",
            "balance BIGINT NOT NULL DEFAULT 0 -- total available (permanent + unexpired)",
            "permanent_balance BIGINT NOT NULL DEFAULT 0 -- never expires",
            "expiring_balance BIGINT NOT NULL DEFAULT 0 -- sum of unexpired time-limited",
            "earliest_expiry TIMESTAMPTZ -- when next credits expire (for efficient expiry job)",
            "created_at TIMESTAMPTZ DEFAULT NOW()",
            "updated_at TIMESTAMPTZ DEFAULT NOW()",
            "UNIQUE(user_id, credit_type_id)"
          ]
        },
        "credit_transactions": {
          "description": "Append-only ledger of all credit changes",
          "columns": [
            "id UUID PRIMARY KEY",
            "user_id TEXT NOT NULL",
            "credit_type_id UUID REFERENCES credit_types(id)",
            "amount BIGINT NOT NULL -- positive = deposit, negative = withdrawal",
            "balance_after BIGINT NOT NULL -- balance after this transaction",
            "transaction_type TEXT NOT NULL -- 'deposit', 'withdrawal', 'expiry', 'refund', 'admin_adjust'",
            "source TEXT NOT NULL -- 'purchase', 'usage', 'admin', 'expiry_job', 'refund'",
            "source_id UUID -- payment_id, api_request_id, etc.",
            "expires_at TIMESTAMPTZ -- for deposits: when these credits expire (NULL = permanent)",
            "description TEXT",
            "created_at TIMESTAMPTZ DEFAULT NOW()",
            "INDEX(user_id, credit_type_id, created_at DESC)"
          ]
        },
        "credit_expiry_batches": {
          "description": "Tracks expiring credit batches for FIFO consumption",
          "columns": [
            "id UUID PRIMARY KEY",
            "user_id TEXT NOT NULL",
            "credit_type_id UUID REFERENCES credit_types(id)",
            "original_amount BIGINT NOT NULL",
            "remaining_amount BIGINT NOT NULL",
            "expires_at TIMESTAMPTZ NOT NULL",
            "source_transaction_id UUID REFERENCES credit_transactions(id)",
            "created_at TIMESTAMPTZ DEFAULT NOW()",
            "INDEX(user_id, credit_type_id, expires_at ASC) -- for FIFO consumption"
          ]
        }
      },
      "product_integration": {
        "new_field": "Product.CreditsSpec map[string]int64 -- {'gpu_time': 100, 'api_credits': 1000}",
        "purchase_flow": [
          "User purchases product with CreditsSpec",
          "RegisterPurchase() calls CreditService.Deposit() for each credit type",
          "Deposit creates transaction + updates balance + optionally creates expiry batch"
        ],
        "expiration_from_product": "Product could specify credits_expiry_days (e.g., 90 days). If set, credits expire 90 days from purchase."
      },
      "service_api": {
        "CreditService": {
          "GetBalance(ctx, userID, creditType)": "Returns current balance (permanent + unexpired)",
          "GetDetailedBalance(ctx, userID, creditType)": "Returns {total, permanent, expiring, earliest_expiry}",
          "Deposit(ctx, userID, creditType, amount, source, sourceID, expiresAt)": "Add credits, create transaction, update balance",
          "Withdraw(ctx, userID, creditType, amount, source, sourceID)": "Deduct credits (FIFO: expiring first), returns ErrInsufficientBalance if not enough",
          "GetTransactionHistory(ctx, userID, creditType, limit, offset)": "Returns transaction ledger",
          "ExpireCredits(ctx)": "Background job to expire old credits and update balances"
        }
      },
      "withdrawal_fifo_logic": [
        "1. Get all unexpired expiry batches for user+creditType, ordered by expires_at ASC",
        "2. Deduct from earliest-expiring batch first",
        "3. If batch depleted, move to next batch",
        "4. After all expiring batches, deduct from permanent balance",
        "5. Update user_credit_balances totals",
        "6. Create transaction record with amount, balance_after"
      ],
      "background_jobs": {
        "credit_expiry_job": {
          "schedule": "Every hour (or daily)",
          "logic": [
            "Find all expiry_batches WHERE expires_at <= NOW() AND remaining_amount > 0",
            "For each: create 'expiry' transaction, set remaining_amount = 0",
            "Update user_credit_balances (recalculate expiring_balance, earliest_expiry)"
          ]
        }
      },
      "endpoints": {
        "user_facing": [
          "GET /v1/me/credits -- list all credit balances for user",
          "GET /v1/me/credits/:type -- get detailed balance for specific credit type",
          "GET /v1/me/credits/:type/transactions -- get transaction history"
        ],
        "admin": [
          "POST /v1/admin/users/:id/credits -- admin grant/adjust credits",
          "GET /v1/admin/users/:id/credits -- view user's credit balances",
          "GET /v1/admin/credit-types -- list all credit types",
          "POST /v1/admin/credit-types -- create new credit type"
        ],
        "internal": [
          "POST /v1/internal/credits/withdraw -- called by API/GPU service to deduct credits"
        ]
      },
      "usage_integration": {
        "pattern": "External services (API gateway, GPU scheduler) call internal endpoint to withdraw credits before allowing operation",
        "example_flow": [
          "User requests GPU job costing 10 credits",
          "GPU service calls POST /v1/internal/credits/withdraw {user_id, credit_type: 'gpu_time', amount: 10, source: 'gpu_job', source_id: job_id}",
          "If success: GPU service proceeds with job",
          "If ErrInsufficientBalance: GPU service rejects with 402 Payment Required"
        ]
      },
      "implementation_steps": [
        "Create migration for credit_types, user_credit_balances, credit_transactions, credit_expiry_batches tables",
        "Create CreditType model",
        "Create UserCreditBalance model",
        "Create CreditTransaction model",
        "Create CreditExpiryBatch model",
        "Create CreditTypeRepo with CRUD operations",
        "Create CreditBalanceRepo with balance operations",
        "Create CreditTransactionRepo for ledger operations",
        "Create CreditService with Deposit/Withdraw/GetBalance logic",
        "Implement FIFO withdrawal logic with expiry batch consumption",
        "Add CreditsSpec field to Product model",
        "Update RegisterPurchase to grant credits from Product.CreditsSpec",
        "Create River job for credit expiry processing",
        "Create GET /v1/me/credits endpoint",
        "Create GET /v1/me/credits/:type endpoint",
        "Create GET /v1/me/credits/:type/transactions endpoint",
        "Create POST /v1/admin/users/:id/credits endpoint",
        "Create POST /v1/internal/credits/withdraw endpoint",
        "Write tests for deposit/withdraw/expiry flows",
        "Write tests for FIFO consumption order",
        "Document credit system in API docs"
      ],
      "passes": false
    },
    {
      "name": "admin-dashboard-metrics-overhaul",
      "category": "feature",
      "description": "Overhaul admin dashboard metrics to provide useful business intelligence",
      "current_state": {
        "endpoint": "GET /v1/admin/metrics?type=dashboard|daily|processor",
        "metrics_provided": [
          "active_users_without_auto_renew - counts active subs where cancelled_at IS NOT NULL (confusing)",
          "active_users_with_auto_renew - counts active subs for CCBill/NMI processors",
          "active_users_with_failing_rebill - counts past_due subscriptions"
        ],
        "daily_metrics": [
          "new_signups - subscriptions created on date",
          "explicit_cancellations - cancelled by user/admin/merchant",
          "failed_rebill_cancellations - cancelled due to expired/failed_payment"
        ],
        "processor_metrics": "Same 3 metrics broken down by processor"
      },
      "problems": [
        "Limited metrics - only subscription counts, no revenue or business KPIs",
        "Confusing naming - 'without_auto_renew' actually means 'cancelled but still in period'",
        "No revenue metrics - MRR, ARR, total revenue, average revenue per user",
        "No churn metrics - churn rate, retention rate, LTV",
        "No Solana support - processor metrics exclude Solana one-time payments",
        "No period comparisons - can't compare this week/month vs previous",
        "No cohort analysis - can't track retention by signup month",
        "No conversion funnel - signups to first payment to recurring",
        "Tests are minimal - just verify response structure, not actual data accuracy"
      ],
      "proposed_metrics": {
        "revenue_metrics": [
          "MRR (Monthly Recurring Revenue) - sum of active subscription monthly values",
          "ARR (Annual Recurring Revenue) - MRR * 12",
          "Total revenue this period - sum of all payments in date range",
          "Revenue by processor - breakdown by CCBill, NMI, Solana",
          "ARPU (Average Revenue Per User) - total revenue / active users"
        ],
        "subscription_metrics": [
          "Total active subscriptions",
          "New subscriptions this period",
          "Cancelled subscriptions this period (with breakdown by reason)",
          "Net subscriber change (new - cancelled)",
          "Subscriptions by status (active, past_due, cancelled)",
          "Subscriptions by product/tier"
        ],
        "churn_metrics": [
          "Monthly churn rate - cancelled / active at start of month",
          "Voluntary churn - user/merchant initiated",
          "Involuntary churn - payment failures",
          "Retention rate by cohort - % of month-N signups still active"
        ],
        "payment_metrics": [
          "Successful payments this period",
          "Failed payments this period",
          "Payment success rate",
          "Refunds issued",
          "Chargebacks received",
          "Average payment amount"
        ],
        "one_time_purchases": [
          "Solana payments count and value",
          "One-time purchase revenue (non-subscription)"
        ]
      },
      "api_design": {
        "dashboard_summary": {
          "endpoint": "GET /v1/admin/metrics/summary",
          "returns": "Key KPIs for dashboard cards (MRR, active subs, churn rate, etc.)"
        },
        "revenue_over_time": {
          "endpoint": "GET /v1/admin/metrics/revenue?start=DATE&end=DATE&granularity=day|week|month",
          "returns": "Time series of revenue data"
        },
        "subscriptions_over_time": {
          "endpoint": "GET /v1/admin/metrics/subscriptions?start=DATE&end=DATE&granularity=day|week|month",
          "returns": "Time series of subscription counts"
        },
        "churn_analysis": {
          "endpoint": "GET /v1/admin/metrics/churn?start=DATE&end=DATE",
          "returns": "Churn breakdown by reason, cohort retention"
        },
        "processor_breakdown": {
          "endpoint": "GET /v1/admin/metrics/processors",
          "returns": "Revenue and counts by processor"
        }
      },
      "implementation_notes": [
        "MRR calculation: sum(price.amount / (price.billing_cycle_days / 30)) for active subs",
        "Need to handle different billing cycles (monthly, quarterly, annual)",
        "Solana one-time payments are not subscriptions - track separately",
        "Consider caching expensive aggregations (refresh every 5 min)",
        "Use PostgreSQL window functions for period-over-period comparisons",
        "May want ClickHouse for historical analytics at scale"
      ],
      "steps": [
        "Define exact metrics needed for admin dashboard UI",
        "Design API response formats for each endpoint",
        "Implement MRR/ARR calculation logic",
        "Implement churn rate calculation",
        "Add revenue aggregation queries",
        "Add one-time purchase tracking (Solana)",
        "Add period comparison support (vs last period)",
        "Add caching layer for expensive queries",
        "Update existing metrics endpoints or create new ones",
        "Write comprehensive tests with seeded data",
        "Document all metrics definitions"
      ],
      "passes": false
    }
  ]
}
