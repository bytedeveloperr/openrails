services:
  # Requires the shared Postgres container (service name `postgres`) to already be running
  # and connected to the `local-doujins` network (e.g., from the doujins-backend stack).
  # Redis-compatible cache (Microsoft Garnet)
  garnet:
    image: ghcr.io/microsoft/garnet:latest
    volumes:
      - garnet_data:/data
    ports:
      - "6382:6379"
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "timeout 1 bash -c 'exec 3<>/dev/tcp/localhost/6379' 2>/dev/null",
        ]
      interval: 2s
      timeout: 5s
      retries: 5
    networks:
      - local-doujins

  # ClickHouse for analytics
  clickhouse:
    image: clickhouse/clickhouse-server:25.8-alpine
    restart: unless-stopped
    ports:
      - "8124:8123"  # HTTP interface
      - "9003:9000"  # Native client (avoid conflicts)
    volumes:
      - ./clickhouse/config.yaml:/etc/clickhouse-server/config.yaml:ro
      - clickhouse_data:/var/lib/clickhouse
      - clickhouse_logs:/var/log/clickhouse-server
    environment:
      CLICKHOUSE_DB: analytics
      CLICKHOUSE_USER: analytics_user
      CLICKHOUSE_PASSWORD: analytics_password
      CLICKHOUSE_DEFAULT_ACCESS_MANAGEMENT: 1
    healthcheck:
      test: ["CMD", "clickhouse-client", "--user=analytics_user", "--password=analytics_password", "--query", "SELECT 1"]
      interval: 10s
      timeout: 10s
      retries: 20
      start_period: 120s
    networks:
      - local-doujins

  # One-shot migrator for ClickHouse. Applies all SQL in migrations/clickhouse
  clickhouse-init:
    # Use the server image; it includes `clickhouse-client`
    image: clickhouse/clickhouse-server:25.8-alpine
    depends_on:
      clickhouse:
        condition: service_healthy
    environment:
      CLICKHOUSE_HOST: clickhouse
      CLICKHOUSE_PORT: "9000"
      CLICKHOUSE_USER: analytics_user
      CLICKHOUSE_PASSWORD: analytics_password
    volumes:
      - ./migrations/clickhouse:/migrations:ro
    # Apply each .sql file once ClickHouse is healthy
    command: >
      sh -lc 'for f in /migrations/*.sql; do [ -e "$${f}" ] || continue; echo "Applying $${f}"; clickhouse-client --host=$$CLICKHOUSE_HOST --port=$$CLICKHOUSE_PORT --user=$$CLICKHOUSE_USER --password=$$CLICKHOUSE_PASSWORD --database analytics --multiquery --queries-file "$${f}" || exit 1; done && echo "ClickHouse migrations complete"'
    restart: "no"
    networks:
      - local-doujins

  # Bootstrap DB: create roles/schema/extensions using superuser
  db-bootstrap:
    image: postgres:17-bookworm
    environment:
      PGPASSWORD: admin_password
    volumes:
      - ./migrations/bootstrap:/bootstrap:ro
    # Supply psql variables used by the SQL file
    command: [
      "psql",
      "-v", "ON_ERROR_STOP=1",
      "-v", "DOUJINS_PW=${DOUJINS_DB_PASSWORD:-doujins_password}",
      "-v", "BILLING_PW=${BILLING_DB_PASSWORD:-billing_password}",
      "-h", "postgres",
      "-U", "admin",
      "-d", "doujins_db",
      "-f", "/bootstrap/00000_baseline_roles.sql"
    ]
    restart: "no"
    networks:
      - local-doujins


  # App migrations: run embedded migrator in the billing image
  # billing-migrate:
  #   build:
  #     context: .
  #     dockerfile: Dockerfile
  #   depends_on:
  #     postgres:
  #       condition: service_healthy
  #     db-bootstrap:
  #       condition: service_completed_successfully
  #   env_file:
  #     - ./.env.docker
  #   environment:
  #     PGOPTIONS: -c search_path=$${DB_SCHEMA:-billing},public
  #   volumes:
  #     - ./config.docker.yaml:/app/config.docker.yaml:ro
  #   command: ["migrate", "-c", "/app/config.docker.yaml"]
  #   restart: "no"
  #   networks:
  #     - local-doujins

  # Billing service (server + in-process workers)
  billing:
    build:
      context: .
      dockerfile: Dockerfile
    # Avoid infinite restart loops if the app panics at startup
    restart: on-failure:3
    depends_on:
      db-bootstrap:
        condition: service_completed_successfully
      # billing-migrate:
      #   condition: service_completed_successfully
      garnet:
        condition: service_healthy
      clickhouse:
        condition: service_healthy
    ports:
      - "2054:2053"  # Public API
    expose:
      - "8060"       # Private API (internal-only)
    env_file:
      - ./.env.docker
    environment:
      PGOPTIONS: -c search_path=$${DB_SCHEMA:-billing},public
    command: ["server", "-c", "/app/config.docker.yaml"]
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:2053/health/live"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    volumes:
      - ./config.docker.yaml:/app/config.docker.yaml:ro
      - billing_spool:/var/lib/doujins-billing/spool
    networks:
      - local-doujins

volumes:
  garnet_data:
  clickhouse_data:
  clickhouse_logs:
  billing_spool:

networks:
  local-doujins:
    external: true
    name: local-doujins
